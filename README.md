# CsrfTest

This project includes a gradle build file and the gradle wrapper.

To build a war file from the command line, simply issue "gradlew clean assemble" at the command line from the root of the project.
A war file will be created in /build/libs.

